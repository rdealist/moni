# Ignore better-sqlite3 (not needed in production)
# It's only used locally with drizzle-kit
ignore-scripts=false
legacy-peer-deps=false
strict-peer-dependencies=false

# Use Node.js 20 (compatible with better-sqlite3)
target=20
